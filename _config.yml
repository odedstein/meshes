title: odedstein-meshes
description: A Computer Graphics Example Mesh Repository
show_downloads: true
remote_theme: pages-themes/minima@v2.5.1
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
exclude: objects/
